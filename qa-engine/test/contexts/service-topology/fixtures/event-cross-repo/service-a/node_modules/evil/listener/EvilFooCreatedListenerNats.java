package listener;

// Fixture: a listener living under node_modules — walk() must SKIP this directory entirely.
// If the guard is missing, this vendored/build-artifact listener would be extracted just like
// a real application listener, producing a spurious extra link for FooCreatedEvent.
public class EvilFooCreatedListenerNats extends ListenerMessageDelegate {
    public void onMessage(Message message) {
        FooCreatedEvent fooCreatedEvent =
            messengerClient.convertMsgToSpecificType(message, FooCreatedEvent.class);
    }
}
