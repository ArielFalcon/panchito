{
  "name": "@panchito/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Operator/value web dashboard for panchito. Built to web/dist and served same-origin by the orchestrator at /app. Consumes @panchito/sdk. Implementation is delegated — this package is the prepared slot.",
  "dependencies": {
    "@panchito/sdk": "*"
  }
}
