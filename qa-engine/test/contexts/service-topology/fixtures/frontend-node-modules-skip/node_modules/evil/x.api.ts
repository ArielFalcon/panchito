// Fixture: a call-site living inside node_modules — walk() must SKIP this directory entirely.
// If the guard is missing, this call-site would be extracted just like real application code,
// producing a spurious ServiceLink/drift/unresolved entry sourced from a vendored dependency.
const BASE_PATH = 'name-orders-api';

const evilApi = {
  listOrders() {
    return this.rest.get(`${BASE_PATH}/orders`);
  },

  rest: null as unknown as { get(p: string): unknown },
};

export default evilApi;
