# Patrones que NUNCA deben llegar al LLM (además de la redacción del sanitizer).
# Un fichero/ruta que cuadre con estos patrones se excluye del contexto.

**/.env
**/.env.*
**/secrets/**
**/*.pem
**/*.key
**/node_modules/**
**/dist/**
